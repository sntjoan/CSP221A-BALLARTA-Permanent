I create a student class with names and numpy scores

I validate scores immedaitely when creating creating a student

I create two custome errors(invalid score error and student record locked error)

I use pandas to clean the raw data 

trim names, split score to numbers, convert to name

I build a student objects one at a time

I stores errors in a failures list with the original row